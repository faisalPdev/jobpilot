# mock  -> everything runs in-browser against a localStorage database (no backend needed)
# http  -> talks to the FastAPI backend at VITE_API_BASE_URL
VITE_API_MODE=mock
VITE_API_BASE_URL=/api
# Target for the dev-server /api proxy when VITE_API_MODE=http
VITE_PROXY_TARGET=http://localhost:8000
